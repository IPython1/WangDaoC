/*@Author JieJie*/
#include <stdio.h>
#include <stdlib.h>
/*
线性表：由N个相同类型的元素组成的有序集合
前驱，后继
线性表的顺序表示  简称顺序表（逻辑上相邻的两个元素在物理位置上也相邻）
插入操作：1.将最后一个元素到第i个元素依次后移一位 2.空出的位置i处放入x 3.线性表的长度+1
删除操作：1.将删除的元素赋值给e 2.将删除位置后的元素依次前移 3.线性表长度-1
*/
#define MaxSize 50 //定义线性表的长度
// typedef struct 
// {
//     ElemType date[MaxSize]; //顺序表的元素
//     int len; //顺序表的当前长度
// }SqList; //顺序表的类型定义

int main()
{   




    system("pause");
    return 0;
}